name: Build Windows

on:
  push:
    branches:
      - main

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install dependencies
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller
          pip install PySide6
          pip install yt-dlp
          pip install python-mpv

      - name: Extract mpv
        shell: pwsh
        run: |
          7z x Assets/mpv/mpv.7z -oAssets/mpv/extracted

      - name: Build Rify
        shell: pwsh
        run: |
          pyinstaller --name Rify --windowed --collect-all PySide6 --hidden-import yt_dlp --add-data "Assets/mpv/extracted;mpv" main.py

      - name: Compress build
        shell: pwsh
        run: |
          Compress-Archive -Path dist/Rify -DestinationPath Rify-Windows.zip

      - name: Upload build
        uses: actions/upload-artifact@v4
        with:
          name: Rify-Windows
          path: Rify-Windows.zip 